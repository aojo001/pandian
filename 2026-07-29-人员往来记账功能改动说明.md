# 人员往来记账功能改动说明

日期：2026-07-29

## 需求理解

在烟草交易功能之外增加独立记账模块。用户可以维护往来人员的姓名、备注和头像，为选中的人员记录“我收钱”或“我付钱”，并查看今日收入、支出、结余及历史记录。

## 技术方案

- 使用独立的 `AccountingStore` 保存人员和记账记录，不与烟草订单混合。
- 数据采用 SharedPreferences + JSON 离线存储，最多保留最近 1000 条记账记录。
- 人员头像通过 Android 系统图片选择器上传，并复制到应用私有目录持久保存。
- 手机采用纵向卡片布局；平板采用人员侧栏 + 记账工作区双栏布局。
- 以 `720dp × 600dp` 作为平板双栏断点，横屏手机继续使用可滚动手机布局。

## 修改文件

- `app/src/main/java/com/pandian/tobacco/AccountingStore.kt`：新增人员、收支方向、记账记录模型和本地持久化。
- `app/src/main/java/com/pandian/tobacco/BookkeepingScreen.kt`：新增人员管理、头像上传、收付款记账、今日统计和记录删除界面。
- `app/src/main/java/com/pandian/tobacco/HomeScreen.kt`：首页新增“记一笔账”快捷入口。
- `app/src/main/java/com/pandian/tobacco/MainActivity.kt`：新增记账页面路由。
- `README.md`：补充记账功能说明。
- `2026-07-29-人员往来记账功能改动说明.md`：记录本轮改动与验证结果。

## 使用流程

1. 首页点击“记一笔账”。
2. 点击“添加”，上传人员图片，填写姓名和备注。
3. 点击人员卡片选中记账对象，也可点击“编辑”更新资料。
4. 选择“我收钱”或“我付钱”。
5. 输入金额与备注，保存这笔账。
6. 页面自动更新今日收入、今日支出和今日结余。
7. 记错的记录可通过右侧删除按钮移除。

## 验证结果

- 已使用 JDK 17 执行 `./gradlew assembleDebug`。
- Kotlin 编译、资源打包和 Debug APK 构建成功。
- 头像上传使用系统图片选择器，不申请额外相册读取权限。
