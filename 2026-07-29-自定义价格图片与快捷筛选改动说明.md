# 自定义价格、图片编辑与快捷筛选改动说明

日期：2026-07-29

## 需求理解

开单时不再选择散户或大户，所有烟草直接使用默认价格或本单自定义价格。同时允许编辑烟草图片，并通过搜索、品牌快捷按钮和“已选”筛选快速找到烟草后结算。

## 修改文件

- `app/src/main/java/com/pandian/tobacco/MainActivity.kt`：移除客户类型选择，新增搜索、快捷筛选、已选筛选、响应式商品网格、图片选择和“去结算”流程。
- `app/src/main/java/com/pandian/tobacco/Models.kt`：商品模型新增本地图片路径。
- `app/src/main/java/com/pandian/tobacco/LedgerStore.kt`：保存商品图片到应用私有目录，并持久化图片路径；新订单不再要求客户类型参数。
- `app/src/main/java/com/pandian/tobacco/ReceiptPrinter.kt`：小票移除散户/大户信息。
- `app/src/main/java/com/pandian/tobacco/HomeScreen.kt`：客户管理入口文案移除散户/大户分类。
- `README.md`：更新当前功能说明。
- `2026-07-29-自定义价格图片与快捷筛选改动说明.md`：记录本轮改动与验证结果。

## 当前业务流程

1. 选择“收烟”或“买烟”。
2. 输入烟草名称，或点击全部、已选、红南京等快捷筛选按钮。
3. 点击加减按钮调整数量。
4. 点击烟草图片，设置默认价、本单自定义价、数量，或从系统相册更换图片。
5. 点击“去结算”，核对各款烟草的小计和合计金额。
6. 确认结算并选择系统打印或蓝牙小票机。

## 数据兼容

- 旧商品没有图片路径时继续使用内置示意图或名称占位卡片。
- 旧订单的客户类型字段仍可读取，但新界面、小票和历史记录不再展示该字段。
- 用户选择的图片会复制到应用私有目录，原相册图片移动或删除后仍可显示。

## 验证结果

- 已使用 JDK 17 执行 `./gradlew assembleDebug`。
- Kotlin 与 APK 编译成功。
- 图片编辑使用 Android 系统相册选择器，不需要额外读取相册权限。
