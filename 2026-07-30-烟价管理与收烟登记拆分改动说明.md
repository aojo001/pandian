# 烟价管理与收烟登记拆分改动说明

日期：2026-07-30

## 需求

- 烟价管理不再与收烟登记共用同一个页面。
- 烟价管理支持新增烟草，以及编辑图片、名称、分类、单位和默认价格。
- 收烟登记增大文字、金额与数量按钮，只负责筛选商品、调整本单价格和结算。

## 修改内容

- 新增独立“商品资料管理”页面，手机和平板采用自适应网格。
- 商品数据新增 `category`（分类）与 `unit`（单位）字段，并兼容旧数据默认值。
- 分类和单位既可快捷选择，也可手动自定义。
- 收烟登记的快捷筛选改为“全部 / 已选 / 分类”，保留名称搜索。
- 收烟登记不再修改商品图片和默认价格，避免基础资料与本单价格混淆。
- 放大收烟/买烟切换、搜索、商品名称、价格、数量按钮、合计金额和结算按钮。
- 结算、历史记录和打印小票显示商品单位；跨单位汇总统一显示为“件”。

## 数据兼容

- 旧商品没有分类或单位时，自动使用“卷烟”和“条”。
- 旧订单读取时同样使用默认分类和单位，不影响已有记录。
- 备份功能保存的是完整商品与订单 JSON，因此新增字段会随备份和恢复一起处理。
