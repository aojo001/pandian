# Android 开发环境安装说明

日期：2026-07-29

## 本次操作

- 安装 Apple Silicon 版本 JDK 17。
- 安装 Android SDK 命令行工具。
- 接受 Android SDK 软件包许可协议。
- 安装 Android SDK Platform 35、Build Tools 35.0.1 和 Platform Tools。
- 配置项目 `local.properties` 指向本机 Android SDK。
- 将用户终端的 `JAVA_HOME` 从原有 Java 8 切换为新安装的 JDK 17。
- 安装 Android Studio Quail 2（2026.1.2 Patch 1，Apple Silicon）。
- 使用实际环境执行 Debug APK 构建验证。

## 安装位置

- JDK 17：`~/Library/Java/JavaVirtualMachines/zulu17.66.19-ca-jdk17.0.19-macosx_aarch64/Contents/Home`
- Android SDK：`~/Library/Android/sdk`
- Android Studio：`/Applications/Android Studio.app`

## 修改文件

- `local.properties`：配置本机 Android SDK 路径；该文件已由 `.gitignore` 排除，不会提交个人路径。
- `app/build.gradle.kts`：统一 Java 与 Kotlin 的 JVM 编译目标为 17。
- `2026-07-29-Android开发环境安装说明.md`：记录本次安装和验证结果。
- `~/.zshrc`：把默认 `JAVA_HOME` 从 `~/.local/jdk/jdk8` 更新到 Zulu JDK 17。

## 验证结果

- JDK：独立 Zulu JDK 17.0.19 安装成功，登录 Shell 中 `java` 与 `javac` 均已验证为 17.0.19；Android Studio 内置 JDK 21.0.10。
- SDK Platform 35：安装成功，目录 `~/Library/Android/sdk/platforms/android-35`。
- Build Tools 35.0.1：安装成功；AGP 构建时另行自动安装了 34.0.0。
- Platform Tools / adb：安装成功，ADB 1.0.41（Platform Tools 37.0.0）。
- Android Studio：官方 SHA-256 校验通过、macOS 代码签名验证通过；版本 2026.1，构建号 `AI-261.25134.95.2612.15914620`。
- Debug APK：`./gradlew assembleDebug` 构建成功，产物位于 `app/build/outputs/apk/debug/app-debug.apk`。

## 构建修复

首次真实编译发现 Java 编译目标为 1.8、Kotlin 编译目标为 17，Gradle 因目标不一致终止。已在 `app/build.gradle.kts` 中把 Java `sourceCompatibility`、`targetCompatibility` 以及 Kotlin `jvmTarget` 统一设置为 17；重新编译通过。
