# 烟收宝 v1.0.2

- 更新下载改用 GitHub Release Asset API，不再直接连接容易超时的 `github.com` 附件页面。
- 自动跳转到 `release-assets.githubusercontent.com` 附件 CDN。
- 增加连接状态、下载进度和最多 3 次自动重试。
- 下载过程中支持取消；失败或取消后可以重新下载。
- 修复 Release 更新说明显示字面量 `\n` 的问题。
