# 第二轮五次迭代改动总结

日期：2026-08-19

## 1. GitHub 在线更新

- App 启动联网检查 GitHub Release。
- 有新版本时弹窗下载并调用系统安装。

## 2. GitHub 自动上传

- 安装并登录 GitHub CLI。
- 项目绑定 `aojo001/pandian`，修改验证后自动提交和推送。

## 3. 结算默认打印

- 结算后优先使用设置中的默认蓝牙打印机。
- 权限、配置或连接异常时回退到打印选择窗口。

## 4. 更新下载可靠性

- 使用 Release Asset API 跳转附件 CDN。
- 支持连接状态、自动重试、取消和重新下载。
- 修复下载任务被 Compose 生命周期错误取消的问题。

## 5. 商品分类与排序

- 烟价管理支持内置分类和自定义分类。
- 支持分类筛选、长按拖拽排序和边缘自动滚动。
- 商品顺序保存到本地并参与备份。
